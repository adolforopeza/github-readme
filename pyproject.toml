# pyproject.toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "github-stats-optimizer"
version = "1.0.0"
dependencies = [
    "requests>=2.31.0",
    "supabase>=2.0.0"
]

[tool.vercel]
entrypoint = "api.index:handler"